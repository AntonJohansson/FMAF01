1.1
	f(0) = i*(0 + i0) = 0.
	f(i) = -1.
1.2
	förlängning bevaras, men linjen är alltid roterad pi/2 grader
	positivt led.
1.3
	Eftersom alla ingående punkter roteras pi/2 grader positive led,
	så bevaras alla vinklar.
1.4
	Förflyttning horisontellt skalar upp strecket (exponentiellt?), medan
	förflyttning vertikalt roterar i positivt led.

	f(z) = e^z = e^(x+iy) = e^x(cosy + isiny).
		fixt y (vågrätt) => f(z) skalas enligt e^x vid en fix vinkel
		på enhetscirkeln.

		fixt x (lodrätt) => f(z) avbildar enhetscirkeln vid en fix
		radie e^x.
1.5
	Cirkeln överlappar sig själv.
1.6
	En linje som inte är axelparallell medför en förändring i radien på
	cirkeln för ökad vinkel.


1.7
	Om z = x + iy

		1/z = 1/|z^2| * (x - iy) = 1/(x^2 + y^2) * (x - iy)

	Linjer längs den imaginära axeln avbildas på räta linjer

		1/(ki) = -(1/k)i
1.8
	Cirkelns radie går mot oändligheten då linjen närmar sig z = 0.
		
		z = x + iy
		(x-iy)/((x+iy)(x-iy)) = (x-iy)/(x^2 - y^2)
1.9
	Det går aldrig att få en komplett cirkel då alla cirklar i avbildningen
	passerar genom z = 0, vilket inte är definierat.
	

1.10
	f(z)	= e^(1/2 * Log(z)) = e^(1/2 * (ln|z| + iArgz))
			= e(1/2*ln|z|) * (cos(Argz/2) + isin(Argz/2))	

	där Argz inte är def på den negativa reella axeln.

	Linjer som skär den negativa reella axeln avbildas som en vertikal hyperbel, dvs, den skär inte den positiva rella axeln i avbildningen.

	(diskontinuitet)

1.11
	cos(Argz/2) <= 0 då Argz/2 >= pi/2 <=> Argz >= pi, vilket inte är inkluderat i principalgrenen för log.

1.12
	Om skärningspunkten ligger precis på den negativa reella axeln så behålls även skärningsvinklarna dock med varje linjesegment.

